# Jellyfin-Rust Environment Configuration
# 
# For persistent configuration, prefer using config.toml instead.
# See config.example.toml for the full config file format.
#
# Environment variables always override config.toml values.
# Copy this file to .env for convenience during development.

# =============================================================================
# DIRECTORIES (XDG Base Directory Specification)
# =============================================================================
# By default, jellyfin-rust follows XDG conventions on Unix:
#   Config: $XDG_CONFIG_HOME/jellyfin-rust (~/.config/jellyfin-rust)
#   Data:   $XDG_DATA_HOME/jellyfin-rust   (~/.local/share/jellyfin-rust)
#   Cache:  $XDG_CACHE_HOME/jellyfin-rust  (~/.cache/jellyfin-rust)
#
# You can override individual directories:
# JELLYFIN_RUST_CONFIG_DIR=/path/to/config
# JELLYFIN_RUST_DATA_DIR=/path/to/data
# JELLYFIN_RUST_CACHE_DIR=/path/to/cache

# Portable mode: use current directory for all files (like previous versions)
# Useful for development or running from a USB drive
# JELLYFIN_RUST_PORTABLE=true

# =============================================================================
# SERVER
# =============================================================================

# Server port (default: 8096)
# JELLYFIN_RUST_PORT=8096

# =============================================================================
# DATABASE
# =============================================================================

# SQLite database URL (overrides default path in data directory)
# Default: sqlite:$DATA_DIR/jellyfin.db?mode=rwc
# DATABASE_URL=sqlite:/custom/path/jellyfin.db?mode=rwc

# =============================================================================
# METADATA PROVIDERS
# =============================================================================

# TMDB API Key (optional)
# Get a free API key at: https://www.themoviedb.org/settings/api
# Without this, only AniList will be used for metadata (anime-focused)
# TMDB_API_KEY=your_tmdb_api_key_here

# Enable Anime Offline Database (default: false)
# When enabled, downloads a ~60MB JSON file on first use to cross-reference
# AniList IDs to AniDB IDs. This is useful for compatibility with some clients.
# ENABLE_ANIME_DB=true

# =============================================================================
# EXTERNAL TOOLS (optional)
# =============================================================================

# Custom path to ffmpeg binary (default: uses system PATH)
# FFMPEG_PATH=/usr/bin/ffmpeg

# Custom path to ffprobe binary (default: uses system PATH)
# FFPROBE_PATH=/usr/bin/ffprobe

# =============================================================================
# LOGGING
# =============================================================================

# Log level configuration (default: jellyfin_rust=debug,tower_http=debug)
# Examples:
#   RUST_LOG=info                    # Basic info logging
#   RUST_LOG=debug                   # Verbose debug logging
#   RUST_LOG=jellyfin_rust=trace     # Very verbose for this crate only
# RUST_LOG=info
